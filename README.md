## Spike for Rules

### Test usage

in ``spike-rules``, execute:

```bash
λ lein repl
```

Load the @marick's testing tools:

```lisp
(use 'midje.repl)
```

Then enable ``autotest``

```lisp
(autotest)
```